//objects are used to group together data 
//objcts are collections of properties 
//properties are a key value pair 
//rather than accesing data via an index, we use custom keys 

let myFirstObject = {
    vote: 7,  //vote is the key; 7 is the value 
    candidates: 5,
    votesCast: 80,
    name:'Nikki'
}
